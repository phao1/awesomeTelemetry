# AwesomeTelemetry UI 设计 v2

> 本文是对 `ui-spec.md`（v1 现状）的优化设计。目标不是补功能，而是**重排信息层次**：把工具从"陈列数据"改为"回答问题"。
>
> 面向实现 agent 编写，含具体的布局、数据结构、交互规则与验收标准。

---

## 0. 设计原则

四条原则，后续所有设计决策都从这里推导。冲突时按序号优先。

### P1 · 结论先行（Conclusion First）

任何一屏的第一个视觉焦点必须是**结论**，不是原始数据。

用户打开工具时脑中只有三个问题：**这个 Agent 为什么慢 / 为什么失败 / Token 花在哪**。v1 把 6 个阶段、19 行指标、4 张图表全部铺开让用户自己归纳——这是把分析工作退回给了用户。

v2 要求：所有已经算得出的判断，直接用文字说出来，原始数据退到第二层。

### P2 · 差异优先（Diff First）

对比场景中，**相等的数据是噪音**。

19 行指标表里若有 12 行差异不足 10%，这 12 行的存在只会稀释另外 7 行的信号。默认只显示有差异的部分，其余收进一行折叠摘要。

### P3 · 语义折叠（Semantic Collapse）

长会话的可读性问题不是靠滚动性能解决的，是靠**把噪音事件聚成有意义的块**解决的。

连续 5 次 `Read` 对分析者来说是 1 个动作而不是 5 个事件；`Edit → Test Fail → Edit` 是 1 个修复循环而不是 3 个事件。折叠后 500 步的会话通常只剩 50–80 行。

### P4 · 每个控件都有功能（No Placeholder）

v1 存在若干占位按钮（折叠 / copy / open / Alerts / 绝对时间下拉）。**占位控件比没有控件更伤信任**——用户点一次没反应，之后会怀疑整个界面。

v2 规定：控件要么实现，要么删除。过渡期可用 `disabled + tooltip 说明原因`，但不允许"点了没反应"。

---

## 1. 信息架构：三层收敛模型

v1 的问题是所有视图都是**平铺**结构。v2 统一为三层：

```
L1  结论层   —— 一屏内可读完，回答"发生了什么"
              自动生成的文字判断 + 3~5 个关键数字
              始终展开，不可折叠

L2  维度层   —— 回答"为什么"
              按分析维度（快/省/质 或 阶段/工具/Token）分组的卡片
              默认展开，可折叠

L3  明细层   —— 回答"证据在哪"
              原始事件、完整指标表、Raw JSON、图表
              默认折叠，按需展开
```

三个主视图的层级映射：

| 视图 | L1 结论层 | L2 维度层 | L3 明细层 |
|------|----------|----------|----------|
| Session | 会话诊断卡（Findings） | 阶段磁贴 + 时间构成条 | 甘特图 / 事件详情 / Raw |
| Agent 总览 | Agent 排名与结论 | 各 Agent 指标卡 | 会话列表下钻 |
| 对比 | 裁决卡（谁赢、赢多少） | 快/省/质三维卡 | 指标表 / 工具表 / 图表 / Transcript |

---

## 2. 全局布局

三栏结构保留，Icon Nav 与 SampleRail 宽度不变。主要改动在 Workspace 顶部。

```
┌──────┬──────────────┬────────────────────────────────────────────────┐
│ Icon │  SampleRail  │  Workspace                                     │
│ Nav  │              │  ┌──────────────────────────────────────────┐  │
│ 56px │  320px       │  │ ViewTabs   [⌘K 搜索]  [模式]  [☾] [导出] │  │← 40px 固定
│      │              │  ├──────────────────────────────────────────┤  │
│      │              │  │ ContextBar（sticky，滚动后出现）         │  │← 36px 条件
│      │              │  ├──────────────────────────────────────────┤  │
│      │              │  │                                          │  │
│      │              │  │  L1 / L2 / L3 内容区（可滚动）           │  │
│      │              │  │                                          │  │
│      │              │  └──────────────────────────────────────────┘  │
└──────┴──────────────┴────────────────────────────────────────────────┘
```

### 2.1 新增：ContextBar（滚动上下文条）

**问题**：对比视图 8+ 区块纵向铺开，用户滚到第 6 屏时已经忘了在比谁、谁领先。

**方案**：内容区滚动超过 240px 时，从顶栏下方滑出一条 36px 的 sticky 条：

- 左侧：当前上下文摘要
  - Session 视图 → `ClaudeCode · 重构 auth 模块 · 8m32s · 142 事件`
  - 对比视图 → `Codex ▍vs▍ ClaudeCode · 综合 2:1 领先 Codex`
- 中部：区块锚点导航（当前区块高亮，点击跳转）
- 右侧：`折叠全部 / 展开全部`

滚动回顶部时自动收起。使用 `IntersectionObserver` 判断当前区块。

### 2.2 新增：全局搜索（⌘K / Ctrl+K）

Trace 工具的刚需，v1 完全缺失。类比 Chrome DevTools 的 filter。

**搜索域**：事件标题、文件路径、Bash 命令、工具名、输入/输出摘要、报错信息。

**行为**：
- 输入即时过滤，甘特图只保留命中行 + 其父分组
- 命中的关键词在事件行与详情面板中高亮（黄底）
- 顶部显示 `12 个匹配 · ↑↓ 跳转 · Esc 退出`
- 支持前缀过滤器：`file:auth.ts`、`tool:Bash`、`status:failed`、`phase:debug`

**实现要求**：搜索索引在会话加载时一次性构建（扁平化事件的可搜字段拼成字符串数组），避免每次输入遍历树。

---

## 3. 视图一：Session 工作台 v2

从上到下：会话诊断卡 → 时间构成条 → 阶段磁贴 → 甘特图 + 详情面板。

### 3.1 【新增·L1】会话诊断卡（SessionFindings）

**这是 v2 最重要的新增组件。** 位置：会话信息卡下方，阶段磁贴上方。

它把工具已经算出的数据翻译成人话。所有 Finding 都可点击 → 定位到对应事件并选中。

```
┌─ 会话诊断 ─────────────────────────────────────────────── 收起 ─┐
│                                                                  │
│  🔴  调试占了 62% 的时间                                         │
│      5m18s / 8m32s，集中在 3 次修复循环，均围绕 auth.ts     →   │
│                                                                  │
│  🟠  单步 npm test 耗时 4m12s                                    │
│      占全会话 31%，是最长的单个事件                          →   │
│                                                                  │
│  🟡  写入前未读取：3 个文件                                       │
│      utils/jwt.ts 等 3 个文件在未读取的情况下被直接改写      →   │
│                                                                  │
│  🔵  Token 集中在系统提示词                                       │
│      输入 Token 中 38% 为重复的系统提示，共发送 14 次        →   │
│                                                                  │
│                                              查看全部 7 条诊断 ▾ │
└──────────────────────────────────────────────────────────────────┘
```

**Finding 数据结构**：

```ts
type Finding = {
  id: string;
  severity: 'critical' | 'warning' | 'notice' | 'info';  // 决定色点
  category: 'time' | 'token' | 'quality' | 'stability';
  title: string;           // 一句话结论，≤20 字，含关键数字
  detail: string;          // 一句话证据，≤40 字
  evidence: {
    eventIds: string[];    // 点击后在甘特图中高亮/定位
    phase?: PhaseId;
    metric?: { key: string; value: number; unit: string };
  };
};
```

**默认规则集（v2 首批 10 条）**：

| # | 规则 | 触发条件 | severity |
|---|------|---------|----------|
| 1 | 单阶段时间占比过高 | 任一阶段 > 50% 总时长 | warning |
| 2 | 存在超长单事件 | 单事件 > 20% 总时长 | warning |
| 3 | 修复循环 | 识别出 ≥2 次 edit→fail→edit | critical |
| 4 | 盲写文件 | 文件被 Write/Edit 前无 Read | warning |
| 5 | 无验证收尾 | verify 阶段事件数 = 0 | critical |
| 6 | 工具失败率高 | 任一工具失败率 > 30% 且调用 ≥3 | warning |
| 7 | 系统提示词重复占比 | 重复系统提示 Token > 30% 输入 | notice |
| 8 | 空转时间过长 | 非模型非工具的间隔 > 15% 总时长 | notice |
| 9 | 首 Token 慢 | TTFT > 5s | notice |
| 10 | 用户被迫多轮介入 | user_prompt 事件 ≥ 4 | warning |

**文案规范**：标题必须包含一个数字，必须是判断句而非描述句。

- ✅ `调试占了 62% 的时间`
- ❌ `调试阶段时长统计`
- ✅ `写入前未读取：3 个文件`
- ❌ `边读边写比率：0.62`

**空态**：`本次会话未发现明显异常。7 项检查全部通过 →`（可展开看通过项）

### 3.2 【新增·L2】时间构成条（TimeCompositionBar）

**问题**：v1 的时间条带只按阶段着色，看不出"时间是被模型吃掉的还是被工具吃掉的"。而这恰恰是"快"维度归因的关键。

**方案**：阶段磁贴上方增加一条 28px 的堆叠条，把总时长按**执行语义**而非阶段拆分：

```
总时长 8m32s
┌──────────────────────────────────────────────────────────────┐
│▓▓▓▓▓▓▓▓▓▓▓▓│████████████████████████████████│░░░░░░░│▒▒▒▒▒▒▒│
└──────────────────────────────────────────────────────────────┘
  模型推理        工具执行                      空转     用户等待
  2m10s (25%)     5m02s (59%)                  48s(9%)  32s(6%)
```

| 分段 | 定义 | 色 |
|------|------|-----|
| 模型推理 | LLM 请求发出到响应结束 | teal-600 |
| 工具执行 | tool_call 起止 | slate-500 |
| 空转 | 无任何事件覆盖的间隔 | slate-200 斜纹 |
| 用户等待 | user_prompt 之间的空档 | amber-300 |

这四段的比例直接决定优化方向：模型慢 → 换模型/调参；工具慢 → 优化工具实现；空转高 → 编排有问题。**这是甘特图里看不出来的信息。**

点击任一段 → 甘特图过滤出对应事件。

### 3.3 阶段磁贴（改造）

保留 6 个磁贴，三处调整：

1. **增加对比基线**：每个磁贴显示该阶段耗时占比 + 与该 Agent 历史均值的偏差（`↑ 比均值高 18%`）。没有基线数据时不显示。
2. **多选过滤**：v1 是单选，改为可多选（点击切换），支持"只看 debug + verify"。
3. **异常标记**：命中 Finding 的阶段，磁贴右上角显示对应 severity 的色点，点击直达该 Finding。

### 3.4 甘特图 v2（TraceGanttTree）

核心改造：**语义折叠**。这是长会话可读性的关键，优先级高于虚拟滚动。

#### 3.4.1 分组规则

事件在渲染前经过一层 `groupEvents()` 变换，产生分组树。规则按序匹配，先命中先生效：

| 优先级 | 组类型 | 匹配规则 | 折叠后标题 |
|--------|--------|---------|-----------|
| 1 | `repair_loop` | `Edit/Write → 失败的 Bash/Test → Edit/Write` 的循环，≥2 轮 | `修复循环 ×3（8 步，2m14s）` |
| 2 | `retry_burst` | 同一工具连续调用 ≥3 次且含失败 | `Bash ×4（3 次失败，18s）` |
| 3 | `read_burst` | 连续 ≥3 个 Read/Glob/Grep，中间无写操作 | `读取 5 个文件（1.2s）` |
| 4 | `write_batch` | 连续 ≥3 个 Edit/Write 命中同一目录 | `编辑 src/auth/ 下 4 个文件（22s）` |
| 5 | `subagent` | subagent_prompt 到其结束之间的全部事件 | `子 Agent：搜索测试用例（12 步，34s）` |

未命中任何规则的事件保持独立行。

#### 3.4.2 分组行的呈现

```
 #   操作                              类型      状态   时间轴
────────────────────────────────────────────────────────────────────
 ▍ 12  ▸ 读取 5 个文件                  分组      ✓      ▬▬              1.2s
 ▍ 17  Edit  src/auth/jwt.ts           tool      ✓        ▬▬▬           3.1s
 ▍ 18  ▾ 🔴 修复循环 ×3                 分组      ✗          ▬▬▬▬▬▬▬▬   2m14s
 ▍  ├ 19  Bash  npm test               tool      ✗          ▬▬          18s
 ▍  ├ 20  Edit  src/auth/jwt.ts        tool      ✓            ▬         2s
 ▍  ├ 21  Bash  npm test               tool      ✗             ▬▬       19s
 ▍  └ …（展开查看剩余 5 步）
 ▍ 27  ▸ Bash ×4（3 次失败）            分组      ✗                ▬▬▬   18s
```

- 分组行的甘特条 = 组内首个事件起点到末个事件终点，颜色用组内主导阶段色
- 分组行左侧色条：若组内含失败，显示失败红；否则用主导阶段色
- `repair_loop` 分组默认**展开**（它是问题所在），其余分组默认**折叠**
- 组标题右侧显示 severity 图标（若关联 Finding）
- 提供全局开关：`语义分组 [开/关]`，关闭后回到 v1 的平铺列表

#### 3.4.3 折叠状态与快捷键

- 表头新增 `全部折叠 / 全部展开`（这个按钮在 v1 是占位的，v2 落实）
- 键盘：`j/k` 上下移动选中；`h/l` 折叠/展开当前组；`Enter` 打开详情；`/` 唤起搜索
- 选中态跟随键盘移动，且自动滚动进视口

#### 3.4.4 时间轴与缩放

- **相对/绝对时间切换落地**：相对显示 `0s / 2m / 4m`；绝对显示 `14:23:05 / 14:25:05`。切换后事件行的起止时间同步变更格式。
- **区间缩放**：在表头时间轴上拖拽选区 → 甘特图缩放到该区间，顶部出现 `已聚焦 2m10s–3m40s ✕` 芯片，点 ✕ 或双击恢复。滚轮 + `⌘` 缩放，拖拽平移。
- 缩放状态与阶段过滤、搜索过滤三者可叠加，任一激活时在甘特图顶部显示过滤芯片组，支持逐个清除。

#### 3.4.5 虚拟滚动

语义折叠后行数通常 < 100，虚拟滚动作为兜底：当**展开后的可见行数 > 200** 时启用（`@tanstack/react-virtual`）。固定行高 32px，避免动态测量。列表最大高度从固定 590px 改为 `min(60vh, 720px)`。

### 3.5 事件详情面板 v2（EventInspector）

| 问题 | v2 方案 |
|------|--------|
| 固定 360px，长内容截断 | 可拖拽（320–720px），宽度记入 localStorage |
| 长 diff / 大 JSON 看不下 | 新增"展开为全屏抽屉"按钮，抽屉宽 80vw |
| Raw JSON 无法定位 | 面板内 `⌘F` 局部搜索，命中高亮 + 计数 |
| copy 按钮无功能 | 落实：复制当前 tab 内容，复制后按钮变 `已复制` 1.5s |
| 窄屏折到下方后体验差 | < 1280px 时改为右侧滑出抽屉（overlay），不再挤压甘特图 |

新增能力：

- **输入/输出 tab 的 diff 视图**增加"仅看变更行"开关，长文件默认折叠未变更段落（显示 `… 42 行未变更 ▾`）
- 详情面板顶部增加**上一步 / 下一步**导航（`←` `→`），无需回甘特图即可顺序浏览
- 步骤编号 `3.2` 增加 tooltip 说明含义（第 3 阶段第 2 步）

---

## 4. 视图二：Agent 总览 v2

改动较小，两处：

### 4.1 【新增·L1】顶部结论条

卡片列表之上加一行结论：

```
5 个 Agent · 87 个会话 · 综合评估

  最快   Codex        平均 4m12s，比最慢快 2.3×
  最省   ClaudeCode   平均 18.2k Token/会话
  最稳   Codex        失败率 4.1%，修复循环 0.3 次/会话
```

### 4.2 卡片改为可排序 + 可对比

- 顶部提供排序维度切换：`按耗时 / 按 Token / 按失败率 / 按验证覆盖率`
- 每张卡片增加 checkbox，勾选两个 → 出现 `对比这两个 →` 悬浮按钮，直接跳对比视图（v1 需要用户自己去 SampleRail 里找）

---

## 5. 视图三：对比分析 v2（CompareBoard）

v1 的 8+ 区块纵向铺开是最大的可用性问题。v2 按三层收敛重排，**区块顺序完全调整**。

### 5.1 新的区块顺序

```
L1 ── ① 裁决摘要（原 ⑨，从末尾提到最前）        始终展开
       ② 关键指标网格（原 ①）                   始终展开

L2 ── ③ 三维度对比：快 / 省 / 质（原 ②）        默认展开，可折叠
       ④ 时间构成与阶段对比（原 ③⑤合并）        默认展开，可折叠

L3 ── ⑤ 详细指标表（原 ⑥，差异优先）            默认折叠
       ⑥ 工具调用分析（原 ⑦）                   默认折叠
       ⑦ 时间线对比（原 ④）                     默认折叠
       ⑧ 可视化图表（原 ⑧，精简）               默认折叠
       ⑨ Transcript                             弹窗，不占版面
```

配合 §2.1 的 ContextBar 提供锚点导航。

### 5.2 ① 裁决摘要（Hero 改造）

v1 的 Hero 是纯装饰的渐变背景 + "A vs B"。v2 让它承载结论：

```
┌──────────────────────────────────────────────────────────────────┐
│  Codex  ▍ vs ▍  ClaudeCode                                       │
│                                                                   │
│  综合 2 : 1  ——  Codex 更快更省，ClaudeCode 代码质量更好          │
│                                                                   │
│  ⚡ 快   Codex 领先        4m12s vs 8m32s，快 2.0×                │
│  💰 省   Codex 领先        18.2k vs 31.5k Token，省 42%           │
│  ✨ 质   ClaudeCode 领先   验证覆盖 100% vs 40%，修复循环 0 vs 3   │
└──────────────────────────────────────────────────────────────────┘
```

每行可点击 → 滚动到对应维度卡。渐变背景保留但降低饱和度，避免和文字抢注意力。

### 5.3 ② 关键指标网格

保留 6 张可点击 KPI 卡与 drill-down，两处调整：

- 卡片上增加**差值徽章**：`Token 18.2k` 下方加 `-42% vs ClaudeCode`，用胜负色
- drill-down 面板从"展开在卡片下方推开布局"改为"展开在网格下方的固定容器"，避免展开时页面跳动

### 5.4 ③ 三维度对比（差异优先）

每个维度卡内的指标逐条对比保留，但应用 P2：

- 差异 < 10% 的指标行默认收进底部 `4 项差异不显著 ▾`
- 剩余指标按**差异幅度降序**排列（差得最多的排最前），不再按固定顺序
- 每行右侧增加差异条：一条以中线为原点、向胜方一侧延伸的水平条，长度 = 相对差异百分比。让"差多少"可视化，而不只是两个数字

维度底部的裁决保留，但改为带归因的一句话：`Codex 领先 —— 主要来自工具调用数少 62%，而非单次调用更快`。

### 5.5 ④ 时间构成与阶段对比（合并）

把 v1 的 ③ 速度指标 和 ⑤ 阶段耗时对比 合并成一个区块，因为它们回答的是同一个问题（时间去哪了）：

- 上半：左右两条 §3.2 的**时间构成条**（模型/工具/空转/等待），直接可比
- 下半：6 行双条形图的阶段对比（保留 v1 设计）
- 侧边：TTFT / TPS 卡片（保留）

### 5.6 ⑤ 详细指标表（差异优先）

19 行表格默认只显示差异 ≥10% 的行，其余折叠为 `12 项差异不显著 ▾`。

新增列首的**差异幅度排序**（默认按差异降序），保留"优势"列的徽章。

### 5.7 ⑧ 可视化图表（精简）

v1 有 4 张 SVG 图。按"决策价值"评估后建议：

| 图表 | 处置 | 理由 |
|------|------|------|
| 雷达图（8 轴） | **保留** | 多维形状一眼可比，是唯一提供"整体轮廓"的图 |
| 环形图（Token 构成） | **替换** | 改为左右两条水平堆叠条，同屏可比且省空间。环形图无法左右对比 |
| 阶段条形图 | **移除** | 与 §5.5 的阶段对比重复 |
| 事件类型分布 | **保留但折叠** | 10 类分布对调试适配器有用，但不是常看项 |

原则：图表要么能支撑一个判断，要么就是装饰。

---

## 6. 全局：色彩体系收敛

### 6.1 现状问题

v1 同屏可出现的色彩来源有四组：6 个阶段色（各含深浅两变体 = 12）+ 10 个 Agent 品牌色 + 4 个状态色 + 2 个 Prompt 独立色 ≈ **28 种颜色**。

结果是颜色失去编码能力——用户无法建立"某个颜色 = 某个含义"的稳定映射。

### 6.2 收敛规则

**一个视觉通道只编码一个维度。**

| 维度 | 编码通道 | 说明 |
|------|---------|------|
| 阶段 | **颜色**（6 色系） | 唯一使用大面积颜色的维度 |
| 状态 | **图标 + 描边**，不用填充色 | ✓ 成功、✗ 失败、⟳ 运行中；失败额外加红色左描边 |
| 事件类型 | **图标**，不用颜色 | 工具/LLM/Prompt 用不同 icon |
| Agent 身份 | **颜色**，但仅限对比视图与总览 | Session 视图内不出现品牌色 |
| 数据来源 | **文字徽章**，中性灰 | Scan/Proxy 是元信息，不该抢色彩预算 |

Prompt 事件（user_prompt / subagent_prompt）不再使用独立色系，改为：保留独立背景底色（极浅中性色）+ 左侧加粗竖条 + 专属图标。它们的区分需求是"结构分隔"，用不着新的色相。

### 6.3 暗色主题

v1 有月亮按钮但无逻辑。v2 要求落地，实现方式：

- 全部颜色改为 CSS 变量，在 `:root` 与 `[data-theme="dark"]` 两处定义
- 阶段色在暗色下降低亮度、提高饱和度（浅色主题的阶段色直接放到深底上会刺眼）
- 跟随系统 + 手动覆盖，选择记入 localStorage
- 甘特条、diff 高亮、JSON 语法高亮三处需单独校对对比度（这是最容易漏的三个地方）

若短期无法完成，**先删除月亮按钮**（P4 原则）。

---

## 7. 设计 Token

供实现时统一引用，不要在组件里写死数值。

### 7.1 间距

`4 / 8 / 12 / 16 / 24 / 32 / 48`，只用这七档。卡片内边距 16，区块间距 24，L1/L2/L3 之间 32。

### 7.2 字号与字重

| 角色 | 字号 | 字重 | 用途 |
|------|------|------|------|
| display | 24 | 600 | Hero 裁决、视图标题 |
| title | 16 | 600 | 区块标题、Finding 标题 |
| body | 14 | 400 | 正文、Finding 详情 |
| label | 12 | 500 | 表头、徽章、磁贴标签 |
| mono-data | 13 | 400 | 数值、时长、Token 数 |
| mono-code | 12 | 400 | 命令、路径、Raw JSON、diff |

**所有数值使用等宽字体并右对齐**——这是数据密集型工具最容易被忽略、但收益最直接的一条。左对齐的比例字体数值列无法快速比较大小。

### 7.3 语义色

```
accent        teal-600     #0d9488   主强调
positive      emerald-600  #059669   胜方/成功
negative      rose-600     #e11d48   败方/失败
warning       amber-600    #d97706   警示
neutral       slate-500    #64748b   中性/相等
```

阶段色沿用 v1 的六色系（蓝/紫/绿/红/橙/粉），但统一为每色系 3 档：`bar`（甘特条）/ `tint`（背景）/ `text`（文字）。

### 7.4 动效

克制。只在三处使用：

- 分组展开/折叠：`height 160ms ease-out`
- 详情面板切换：`opacity 120ms`
- 甘特图缩放：`transform 200ms ease-out`

其余一律无动画。`prefers-reduced-motion: reduce` 时全部关闭。

---

## 8. 文案规范

界面文字是设计材料，不是装饰。三条硬规则：

1. **用用户认知的词，不用系统实现的词**
   - ✅ `写入前未读取` ❌ `边读边写比率异常`
   - ✅ `修复循环` ❌ `repair_loop_detected`

2. **动作按钮说明发生什么，且全流程同名**
   - ✅ `导出报告` → toast `报告已导出` ❌ `提交` → toast `操作成功`

3. **空态和错误态给方向，不给情绪**
   - ✅ `未选择会话。从左侧列表选一个开始分析。`
   - ❌ `暂无数据 😢`
   - ✅ `解析失败：第 1420 行 JSON 格式错误。查看日志 →`
   - ❌ `加载出错了，请稍后重试`

---

## 9. 实施优先级

按"信息层次收益 / 实现成本"排序。**P0 三项全部只依赖已有数据，不需要新的采集能力。**

### P0 — 改变工具价值的三件事

| # | 项 | 对应章节 | 预估 |
|---|-----|---------|------|
| 1 | 甘特图语义折叠（含修复循环识别） | §3.4 | 大 |
| 2 | 会话诊断卡 Findings（10 条规则） | §3.1 | 中 |
| 3 | 对比视图三层收敛 + 差异优先 | §5 | 中 |

### P1 — 显著提升效率

| # | 项 | 对应章节 |
|---|-----|---------|
| 4 | 全局搜索 ⌘K | §2.2 |
| 5 | 时间构成条（模型/工具/空转/等待） | §3.2 |
| 6 | 详情面板可拖拽 + 全屏抽屉 + 局部搜索 | §3.5 |
| 7 | 键盘导航 | §3.4.3 |
| 8 | 清理占位控件（删除或实现） | §0-P4 |
| 9 | ContextBar 滚动上下文 | §2.1 |

### P2 — 体验补齐

| # | 项 | 对应章节 |
|---|-----|---------|
| 10 | 相对/绝对时间切换 | §3.4.4 |
| 11 | 甘特图区间缩放 | §3.4.4 |
| 12 | 暗色主题 | §6.3 |
| 13 | 色彩体系收敛 | §6.2 |
| 14 | 虚拟滚动 | §3.4.5 |
| 15 | Agent 总览排序与快捷对比 | §4 |

---

## 10. 验收标准

实现完成后需满足：

| # | 标准 | 验证方式 |
|---|------|---------|
| 1 | 500 事件会话，语义折叠后首屏可见行数 ≤ 80 | 取一个真实长会话计数 |
| 2 | 打开任一会话，3 秒内能说出"时间花在哪、有没有问题" | 找一个没用过的同事实测 |
| 3 | 对比视图首屏（不滚动）能读出胜负与主要差距 | 截图检查 |
| 4 | 界面上不存在点击无反应的控件 | 逐个点击清单 |
| 5 | 2000 事件会话滚动帧率 ≥ 50fps | Performance 面板 |
| 6 | 全键盘可完成：选会话 → 浏览事件 → 看详情 → 导出 | 不碰鼠标走一遍 |
| 7 | 暗色主题下 diff / JSON 高亮 / 甘特条对比度 ≥ 4.5:1 | 对比度检查工具 |
| 8 | 每个 Finding 都能点击定位到对应事件 | 逐条点击 |

---

## 附：与 v1 的差异摘要

| v1 | v2 |
|----|----|
| 平铺所有数据 | 三层收敛（结论/维度/明细） |
| 用户自己归纳结论 | 诊断卡直接给出结论 |
| 事件逐条平铺 | 语义分组折叠 |
| 时间只按阶段着色 | 增加执行语义拆分（模型/工具/空转/等待） |
| 对比视图裁决在最后 | 裁决提到第一屏 |
| 19 行指标全显示 | 差异 <10% 默认折叠 |
| 4 张图表 | 2 张（雷达 + 堆叠条），其余移除或折叠 |
| 28 种颜色同屏 | 一通道一维度，颜色只编码阶段与 Agent |
| 若干占位按钮 | 要么实现要么删除 |
| 无搜索 | 全局 ⌘K + 前缀过滤器 |
| 鼠标驱动 | 全键盘可用 |
